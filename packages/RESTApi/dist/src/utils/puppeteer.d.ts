import { AppServer } from '../../types';
export interface MouseLocation {
    x: number;
    y: number;
}
export declare function puppeteerSocketServer(fastify: AppServer): Promise<void>;
//# sourceMappingURL=puppeteer.d.ts.map